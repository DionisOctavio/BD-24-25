SELECT APELLIDO
FROM EMPLE
WHERE DEPT_NO = 10;



TODOS LOS EMPLEADOS DE LA EMPRESA QUE COBREN MAS DE 2500

SELECT COUNT(APELLIDO)
FROM EMPLE
WHERE SALARIO+NVL (COMISION,0) > 2500;



QUIERO SABER LOS OFICIOS QUE HAY EN EL DEPT 30

SELECT DISTINCT OFICIO
FROM EMPLE
WHERE DEPT_NO = 30;

SELECT COUNT(DISTINCT OFICIO) AS OFICIOS_DPT_30
FROM EMPLE
WHERE DEPT_NO = 30;




NOTA MAXIMA 1 EVAU

SELECT MAX(NOTA1)
FROM NOTAS_ALUMNOS;




NOTA MAXIMA LUIS BENITO 

SELECT GREATEST(NOTA1, NOTA2, NOTA3)  // ESTO ES LA MAXIMA DE TODO LO QUE QUIERAS PONER
FROM NOTAS_ALUMNOS
WHERE NOMBRE_ALUMNO = 'Benito Martín, Luis';


NOTA MINIMO LUIS BENITO 

SELECT LEAST(NOTA1, NOTA2, NOTA3)  // ESTO ES LA MINIMA DE TODO LO QUE QUIERAS PONER
FROM NOTAS_ALUMNOS
WHERE NOMBRE_ALUMNO = 'Benito Martín, Luis';

