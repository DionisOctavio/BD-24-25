CREATE TABLE PERSONA (
	id_persona NUMBER,
	dni VARCHAR2(15),
	nombre VARCHAR2(50),
	CONSTRAINT PK_PERSONA PRIMARY KEY (id_persona),
	CONSTRAINT UQ_DNI UNIQUE (dni),
	CONSTRAINT NN_DNI CHECK (dni IS NOT NULL),
	CONSTRAINT NN_NOMBRE CHECK (nombre IS NOT NULL)
);

CREATE TABLE PEÑISTA (

);

CREATE TABLE PEÑA (
	id_peña NUMBER,
	nombre VARCHAR2(50),
	año_fundacion NUMBER(4),
	direccion VARCHAR(100),
	CONSTRAINT PK_PEÑA PRIMARY KEY (id_peña),
	CONSTRAINT NN_NOMBRE CHECK (nombre IS NOT NULL),
	CONSTRAINT CK_AÑO_FUNDACION CHECK (año_fundacion IS NOT NULL), 

(ANYO FUNDACION BETWEEN 1950 AND 2025)
	CONSTRAINT CK_DIRECCION CHECK (nombre IS NOT NULL),

);